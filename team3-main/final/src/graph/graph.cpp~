#include <iostream>
#include "graph.h"

using namespace std;

graph::graph () {
  
  
  
  /* Reserve spaces for fifos and acotrs in the graph*/
  fifos.reserve(FIFO_COUNT);
  actors.reserve(ACTOR_COUNT);
  descriptors.reserve(ACTOR_COUNT);

}

void graph::scheduler(){
 
}

graph::~graph() {}
