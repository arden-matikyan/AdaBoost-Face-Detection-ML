Source directory of project.

Contains the actor and graph subdirectories as well as the util2 tools for classification. 


Hardcoded values. 
1a) Within file_sink, we specify the count of images that are tested first which are known to be faces.
1b) Every following image is assumed not a face. 
2) We also specify the number of images we will be testing within the runme. 
3) The txt_img_read also requires the known number of postive samples to expect. 


